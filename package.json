{
	"name": "to-do",
	"version": "1.0.0",
	"description": "",
	"scripts": {
		"build": "docker-compose up --build",
		"start": "docker-compose --env-file .env up && docker logs ebk-back -f"
	},
	"keywords": [],
	"author": "",
	"license": "ISC"
}
